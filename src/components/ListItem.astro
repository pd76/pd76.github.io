---
interface Props {
  url: string;
  title: string;
  date: string;
  topic: string
}

const { url, title, date, topic } = Astro.props
---
<li>
<a href={url}>
   <div>
     <span>{title}</span>
   </div>
   <span class="date">{date}</span>
 </a>
</li>

